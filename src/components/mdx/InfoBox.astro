---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<div class="my-6 rounded-lg border border-gray-200 bg-gray-50 p-5">
  <p class="mb-2 font-semibold text-gray-900">{title}</p>
  <div class="text-gray-700">
    <slot />
  </div>
</div>
